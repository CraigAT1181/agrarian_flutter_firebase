import 'package:flutter/material.dart';

class Allotment extends StatelessWidget {
  const Allotment({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Allotment Screen');
  }
}
